[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ecc-micromechanics"
version = "0.2.1"
description = "ECC micromechanics parameter calculator and PSH criterion evaluator"
readme = "README.md"
license = { text = "Proprietary" }
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.1",
    "scipy>=1.11",
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-qt>=4.4",
    "ruff>=0.4",
    "mypy>=1.9",
    "pandas-stubs",
]

[project.scripts]
ecc-calc = "main:main"

[tool.setuptools]
py-modules = ["main"]

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "models*", "ui*", "utils*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "N", "ANN"]
ignore = [
    "ANN201",
    "ANN202",
]

[tool.ruff.lint.per-file-ignores]
# Tests prioritize behavioral clarity over production type annotations.
"tests/*" = ["ANN", "E741"]
# Public scientific parameter names intentionally mirror notation used in the UI/docs.
"core/engine.py" = ["N815"]
# `l` denotes embedment length in the published micromechanics equations.
"core/simulation.py" = ["E741"]
# Qt virtual methods must retain Qt's camelCase method names. Long UI/CSS strings are
# clearer when kept intact rather than split solely for a line-length rule.
"ui/main_window.py" = ["N802", "E501"]
"ui/plot_widgets.py" = ["E501"]
"ui/result_table_model.py" = ["N802", "ANN401"]

[tool.mypy]
python_version = "3.10"
strict = true
implicit_reexport = false

[[tool.mypy.overrides]]
module = ["scipy.*", "matplotlib.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
